var expect = require('expect');

// describe is just visual function it just formats your function to be more readable
describe('App', () => {
  it('should properly run tests', () => {
    expect(1).toBe(1);
  });
})
